---
import { services, getPopularServices, categoryLabels, formatPrice } from '@config/services';
import { getWhatsAppUrl } from '@config/site';
import ServiceCard from '@components/ui/ServiceCard.astro';
import Button from '@components/ui/Button.astro';

const popularServices = getPopularServices();
const whatsappUrl = getWhatsAppUrl('Hola! Me interesa conocer más sobre sus servicios');
---

<section id="servicios" class="py-20 md:py-28 bg-gray-50">
  <div class="container-custom">
    <!-- Header -->
    <div class="text-center max-w-2xl mx-auto mb-12" data-aos="fade-up">
      <h2 class="font-heading text-3xl md:text-4xl font-bold text-gray-900 mb-4">
        Nuestros Servicios
      </h2>
      <p class="text-gray-600 text-lg">
        Servicios profesionales de uñas con los mejores materiales. Todos los precios incluyen
        desplazamiento.
      </p>
    </div>

    <!-- Popular Services -->
    <div class="mb-12">
      <h3
        class="text-xl font-heading font-semibold text-gray-800 mb-6 text-center"
        data-aos="fade-up"
      >
        Más Populares
      </h3>
      <div class="grid md:grid-cols-3 gap-6">
        {
          popularServices.map((service, index) => (
            <ServiceCard service={service} delay={index * 100} />
          ))
        }
      </div>
    </div>

    <!-- All Services Grid -->
    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6 mb-12">
      {
        services
          .filter((s) => !s.popular)
          .map((service, index) => <ServiceCard service={service} delay={index * 50} />)
      }
    </div>

    <!-- Additional Info -->
    <div
      class="bg-white rounded-2xl p-6 md:p-8 shadow-md border border-gray-100"
      data-aos="fade-up"
    >
      <div class="grid md:grid-cols-2 gap-8">
        <!-- Decoración -->
        <div>
          <h4 class="font-heading font-semibold text-lg text-gray-900 mb-4">
            Decoración y Diseños
          </h4>
          <ul class="space-y-2 text-gray-600">
            <li class="flex justify-between">
              <span>Diseño Francesa (Full Set)</span>
              <span class="font-semibold text-primary-700">+{formatPrice(10000)}</span>
            </li>
            <li class="flex justify-between">
              <span>Baby Boomer (Full Set)</span>
              <span class="font-semibold text-primary-700">+{formatPrice(15000)}</span>
            </li>
            <li class="flex justify-between">
              <span>Efectos (Ojo de Gato, Aurora)</span>
              <span class="font-semibold text-primary-700">+{formatPrice(10000)}</span>
            </li>
            <li class="flex justify-between">
              <span>Toque de Diseño (2-4 uñas)</span>
              <span class="font-semibold text-primary-700">+{formatPrice(12000)}</span>
            </li>
            <li class="flex justify-between">
              <span>Diseño Medio (6-8 uñas)</span>
              <span class="font-semibold text-primary-700">+{formatPrice(18000)}</span>
            </li>
            <li class="flex justify-between">
              <span>Diseño Avanzado</span>
              <span class="font-semibold text-primary-700">Se cotiza</span>
            </li>
          </ul>
        </div>

        <!-- Adicionales -->
        <div>
          <h4 class="font-heading font-semibold text-lg text-gray-900 mb-4">
            Servicios Adicionales
          </h4>
          <ul class="space-y-2 text-gray-600">
            <li class="flex justify-between">
              <span>Retiro Semipermanente (sin set nuevo)</span>
              <span class="font-semibold text-primary-700">{formatPrice(10000)}</span>
            </li>
            <li class="flex justify-between">
              <span>Retiro de Sistemas (Acrílico/Polygel)</span>
              <span class="font-semibold text-primary-700">{formatPrice(25000)}</span>
            </li>
            <li class="flex justify-between">
              <span>Reparación de uña (c/u)</span>
              <span class="font-semibold text-primary-700">{formatPrice(8000)}</span>
            </li>
          </ul>

          <div class="mt-6 p-4 bg-accent-50 rounded-xl border border-accent-200">
            <p class="text-sm text-accent-800">
              <strong>Nota:</strong> El retiro de semipermanente es
              <span class="font-semibold">CORTESÍA</span> si te realizas un nuevo set conmigo.
            </p>
          </div>
        </div>
      </div>
    </div>

    <!-- CTA -->
    <div class="text-center mt-12" data-aos="fade-up">
      <p class="text-gray-600 mb-4">¿Tienes dudas sobre algún servicio?</p>
      <Button variant="whatsapp" size="lg" href={whatsappUrl} external>
        <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
          <path
            d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"
          ></path>
        </svg>
        Consultar por WhatsApp
      </Button>
    </div>
  </div>
</section>
