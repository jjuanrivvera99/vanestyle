---
import type { Service } from '@config/services';
import { formatPrice } from '@config/services';

export interface Props {
  service: Service;
  delay?: number;
}

const { service, delay = 0 } = Astro.props;
---

<div
  class="bg-dark-tertiary rounded-2xl p-6 shadow-md shadow-black/20 hover:shadow-xl transition-all duration-300 border border-dark-border hover:border-primary-700 group"
  data-aos="fade-up"
  data-aos-delay={delay}
>
  <div class="flex justify-between items-start mb-3">
    <h3
      class="font-heading font-semibold text-lg text-dark-text group-hover:text-primary-400 transition-colors"
    >
      {service.name}
    </h3>
    {
      service.popular && (
        <span class="bg-accent-900/30 text-accent-400 text-xs font-semibold px-2.5 py-1 rounded-full">
          Popular
        </span>
      )
    }
  </div>

  <p class="text-dark-text-muted text-sm mb-4 line-clamp-2">
    {service.description}
  </p>

  <div class="flex justify-between items-center">
    <div class="flex items-baseline gap-1">
      {service.priceFrom && <span class="text-sm text-dark-text-muted">Desde</span>}
      <span class="font-heading font-bold text-2xl text-primary-400">
        {formatPrice(service.price)}
      </span>
    </div>

    {
      service.duration && (
        <span class="text-sm text-dark-text-muted flex items-center gap-1">
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"
            />
          </svg>
          {service.duration}
        </span>
      )
    }
  </div>
</div>
